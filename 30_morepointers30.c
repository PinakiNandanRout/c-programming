#include <stdio.h>
int main ()
 {
//     int *ptr = NULL; //null pointer
//     printf("The value inside variable ptr is: %d \n",ptr);

//     void *p = NULL;
//     printf("the size of pointer is: %d\n",sizeof(p)); //size of p depends on compiler
// the size of pointer is 8, so we can point as integer float ,char 



    int *pw;//wild pointer
      printf("\n%d",pw);

    return 0;
}